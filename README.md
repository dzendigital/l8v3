## clone && run next command to install project:

- composer install
- npm run dev

- create .env from .evn.example

## configuration:

- set up mail config in .env
- set up database config in .env

- run "artisan migrate" (artisan migrate:fresh)

